# 🔭 Colab Spectral Fitting Project

โปรเจคสำหรับวิเคราะห์สเปกตรัมรังสีเอกซ์ (X-ray Spectral Fitting) บน Google Colab ออกแบบมาให้ใช้งานง่าย ปรับแต่งได้ และรันได้ทันทีโดยไม่ต้องติดตั้งโปรแกรมหนักเครื่อง

## 📂 โครงสร้างโปรเจค

```
Colab_Spectral_Fitting/
├── data_loader.py           # 📊 Core Logic: ไฟล์หลักสำหรับอ่านข้อมูลและคำนวณฟิสิกส์
├── spectral_analysis.ipynb  # 📓 User Interface: Notebook สำหรับรันการวิเคราะห์
├── Models/                  # 📦 Spectral Models Library
│   ├── __init__.py          # ตัวจัดการ Model รวม (Combined Model)
│   ├── powerlaw.py          # Power-law continuum
│   ├── blackbody.py         # Blackbody emission
│   ├── tbabs.py             # Photoelectric absorption
│   ├── gaussian.py          # Gaussian line
│   └── reflection.py        # Reflection component
└── README.md                # 📖 คู่มือการใช้งาน
```

## 🚀 วิธีใช้งาน (Quick Start)

1. **Upload Folder**: อัปโหลดโฟลเดอร์ `Colab_Spectral_Fitting` ทั้งหมดไปที่ Google Drive ของคุณ
2. **Open Notebook**: เปิดไฟล์ `spectral_analysis.ipynb` ใน Google Colab
3. **Mount Drive**: รัน Cell แรกเพื่อเชื่อมต่อ Google Drive
4. **Config & Run**:
   - **Section 1**: กำหนด Energy Range (`0.3 - 10.0 keV`) และ Path ไฟล์ข้อมูล
   - **Section 2**: เลือก Model ที่ต้องการใช้ (Uncomment) และกำหนดค่า Parameter เริ่มต้น
   - **Section 3-5**: รันตามลำดับเพื่อดูผลลัพธ์

---

## 🔍 คำอธิบายการทำงานเชิงลึก (Process Logic)

ส่วนนี้อธิบายการทำงานภายในของโค้ดในแต่ละขั้นตอน เพื่อความเข้าใจที่ชัดเจน

### 1. Load & Prepare Data (การอ่านและเตรียมข้อมูล)
ในขั้นตอนนี้ Notebook จะเรียกใช้ฟังก์ชันจากไฟล์ `data_loader.py` ดังนี้:

*   **1.1 อ่านไฟล์ข้อมูล (`read_spectrum_file`)**
    *   **คำสั่ง:** `source_spec = read_spectrum_file(PATH)`
    *   **การทำงาน:** ระบบจะเปิดไฟล์ FITS อ่าน extension `SPECTRUM` เพื่อดึงข้อมูลดิบ ได้แก่:
        *   `COUNTS`: จำนวนอนุภาคโฟตอนที่วัดได้
        *   `CHANNEL`: เลขช่องรับสัญญาณ
        *   `EXPOSURE`: ระยะเวลาที่บันทึกภาพ (วินาที)
    *   **คืนค่า (Return):** Object `SpectrumData` ที่เก็บข้อมูลข้างต้น

*   **1.2 ลบสัญญาณรบกวน (`subtract_background`)**
    *   **คำสั่ง:** `subtract_background(source_spec, bkg_spec)`
    *   **การทำงาน:**
        1.  คำนวณ **Scaling Factor** โดยดูจากขนาดพื้นที่ (`BACKSCAL`) และเวลาถ่ายภาพ (`EXPOSURE`) ของ Source เทียบกับ Background
        2.  ปรับค่า Background: `Bkg_Scaled = Bkg_Counts * Scale_Factor`
        3.  ลบค่า: `Net_Counts = Source_Counts - Bkg_Scaled`
    *   **คืนค่า (Return):** `net_counts` (ค่าสุทธิ) และ `net_error` (ค่าความคลาดเคลื่อนที่คำนวณใหม่ตามหลัก Error Propagation)

*   **1.3 จัดการช่วงพลังงาน (`filter_energy_range`)**
    *   **คำสั่ง:** `filter_energy_range(..., e_min, e_max)`
    *   **การทำงาน:**
        1.  นำข้อมูล ARF/RMF มาสร้างแกน X (Energy Array)
        2.  **Alignment Check:** ตรวจสอบว่าจำนวนช่องข้อมูล (Data Bins) เท่ากับจำนวนช่องพลังงาน (Energy Bins) หรือไม่ ถ้าไม่เท่าจะทำการตัดส่วนเกิน (Truncate) ให้อัตโนมัติ
        3.  **Filtering:** ตัดข้อมูลทั้งหมดทิ้ง ให้เหลือเฉพาะในช่วง `e_min` ถึง `e_max`
    *   **คืนค่า (Return):** ข้อมูลที่พร้อมสำหรับการ Fit (Cleaned Data)

### 2. Run Spectral Fitting (การวิเคราะห์กราฟ)
กระบวนการ Fit เกิดจากการวนลูปหาค่าพารามิเตอร์ที่ทำให้ "กราฟทฤษฎี" ตรงกับ "กราฟจริง" มากที่สุด

*   **2.1 สร้างโมเดลจำลอง (`combined_model`)**
    *   **เรียกจาก:** `Models/__init__.py`
    *   **การทำงาน:** นำสูตรทางฟิสิกส์มารวมกันตามที่ผู้ใช้เลือกในตัวแปร `SELECTED_MODELS`
    *   **ตัวอย่าง:** ถ้าเลือก `powerlaw + tbabs` สูตรจะเป็น:
        $$ Flux(E) = (N \cdot E^{-\Gamma}) \times e^{-N_H \sigma(E)} $$

*   **2.2 แปลงเป็นค่าที่วัดได้ (`fold_model_through_response`)**
    *   **เรียกจาก:** `data_loader.py`
    *   **การทำงาน:** กราฟทฤษฎี (Flux) ไม่สามารถเทียบกับกราฟจริง (Counts) ได้โดยตรง ต้องคูณด้วย "พื้นที่รับแสง" (Effective Area - ARF) และความกว้างช่องพลังงาน (dE)
    *   **สูตร:** `Predicted_Rate = Model_Flux × ARF × dE`
    *   **สำคัญ:** ฟังก์ชันนี้จะกรอง ARF ให้ตรงกับ Energy Range โดยอัตโนมัติก่อนคำนวณ

*   **2.3 คำนวณความแม่นยำ (`calculate_chi_squared`)**
    *   **การทำงาน:** เปรียบเทียบค่าจริง ($O$) กับค่าทฤษฎี ($E$) โดยใช้สูตร Chi-Squared:
        $$ \chi^2 = \sum \frac{(O - E)^2}{\sigma^2} $$
    *   **เป้าหมาย:** ยิ่งค่า $\chi^2$ ต่ำ แสดงว่ากราฟฟิตได้ดี

---

## 📦 รายละเอียด Spectral Models

Model ที่มีให้เลือกใช้งานในโฟลเดอร์ `Models/`:

| Model | ไฟล์ | คำอธิบาย | พารามิเตอร์หลัก |
|-------|------|----------|-----------------|
| **Power-law** | `powerlaw.py` | การแผ่รังสี Continuum พื้นฐานของ AGN | `pho_index` (ความชัน), `norm` (ความเข้ม) |
| **Blackbody** | `blackbody.py` | การแผ่รังสีความร้อน (Thermal) | `kT` (อุณหภูมิ), `norm` |
| **TBabs** | `tbabs.py` | การดูดกลืนแสงโดยแก๊สระหว่างดาว | `nH` (ความหนาแน่นแก๊ส) |
| **Gaussian** | `gaussian.py` | เส้นการแผ่รังสีจำเพาะ (Emission Line) เช่น Fe Kα | `line_energy`, `sigma`, `norm` |
| **Reflection** | `reflection.py` | การสะท้อนกลับของแสง X-ray (PEXRAV simplified) | `refl_norm`, `photon_index` |

## 📚 Algorithms Reference

- **Statistic**: Chi-Squared ($\chi^2$) minimization
- **Optimization**:
    - `Brute Force`: Grid Search ตามช่วงที่กำหนด (ละเอียดแต่ช้า)
    - `Scipy Minimize`: L-BFGS-B Algorithm (เร็วและแม่นยำ)
- **Error Propagation**: Poisson statistics ($\sigma = \sqrt{N}$) for count data

## 🛠️ Dependencies
- `numpy`: คำนวณตัวเลขและ Array
- `matplotlib`: วาดกราฟ
- `astropy`: อ่านไฟล์ FITS
- `scipy`: ทำ Optimization

---
*Created for AGN Spectral Analysis Research*
